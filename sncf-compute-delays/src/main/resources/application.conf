kafka {
  bootstrap.servers = "localhost:9092"
  schema.registry.url = "http://localhost:8081"
  unique.disruption.topic.name = "unique.sncf-disruptions"
  disruption.delays.topic.name = "sncf-disruptions-computed-delays"

  streams.application.id = "sncf-compute-delays"
}

